# Maximum memory usage (adjust based on your needs)
maxmemory 100mb
maxmemory-policy allkeys-lru # Evict least recently used keys

# Enable logging
logfile "/var/log/redis/redis.log"
loglevel notice

# Disable RDB snapshots for persistence
save ""

# Enable append-only mode (AOF) for data durability
appendonly yes
